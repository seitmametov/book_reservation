server:
  port: 8080

spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
    driver-class-name: org.postgresql.Driver


  jpa:
    hibernate:
      ddl-auto: update   # ⬅️ ВАЖНО: создаёт / обновляет таблицы
    show-sql: true
    properties:
      hibernate:
        format_s_
library:
  reservation:
    max-active: 3

minio:
  endpoint: http://localhost:9000
  access-key: minio
  secret-key: minio123
  bucket: book-covers


