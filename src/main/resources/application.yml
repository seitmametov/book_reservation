server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver

  mail:
    host: smtp.gmail.com
    port: 587
    username: 0anade0@gmail.com
    password: xzzehazmsvflghpr # ПРЯЧЬ ЭТО ПРИ ПУШЕ В ГИТ!
    properties:
      mail:
        smtp:
          auth: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
          starttls:
            enable: true
            required: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  # Вынес mustache из-под minio на уровень spring
  mustache:
    expose-request-attributes: true

minio:
  endpoint: http://localhost:9000
  access-key: minio
  secret-key: minio123
  bucket-name: book-covers # Это будет дефолтный бакет для книг

library:
  reservation:
    max-active: 3